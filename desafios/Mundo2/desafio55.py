# Faça um programa que leia o peso de cinco pessoas. No final, mostre qual foi o maior e o menor peso lidos.


nums = 0
for i in range(1, 6):
    peso = (input(f'Digite o peso da {i}ª pessoa:'))
    # pass
    # peso = [input(f'Insira o peso da {i+1}ª pessoa: Kg '),
    #         input(f'Insira o peso da {i+2}ª pessoa: Kg '),
    #         input(f'Insira o peso da {i+3}ª pessoa: Kg '),
    #         input(f'Insira o peso da {i+4}ª pessoa: Kg '),
    #         input(f'Insira o peso da {i+5}ª pessoa: Kg ')]
    for p in peso:
        num = float(peso)
        nums = peso + peso
        print(peso)
    # pesos = [peso]
    # for p in range(1, peso):
    #     print(peso)
# else:
#     print(pesos)
    # print(f'\n- O menor peso lido foi: \033[1m{min(peso)}Kg\033[m.\n'
    #       f'- O maior peso lido foi: \033[1m{max(peso)}Kg\033[m.')
    # print(f'\n- O menor peso lido foi: {min(peso)}Kg.\n'
    #       f'- O maior peso lido foi: {max(peso)}Kg.')
    print(nums)


# RASCUNHO:

# peso1 = float(input('Insira seu peso: Kg '))
# peso2 = float(input('Insira o seu peso: Kg '))
# peso3 = float(input('Insira o seu peso: Kg '))
# peso = [peso1, peso2, peso3]
# print(min(peso), max(peso))
# pesos = list()
# menor = []
# lista = [input(), input(), input(), input()]
# print(min(lista), max(lista))
    # peso = [input(f'Insira o peso da {i}ª pessoa: Kg '), []]
    # for p in peso:
    #     print(p)
# lista =
# for p in list():
# peso += i
# peso = peso
#     print(peso)
# for p in peso:
#     print(p)
# print(lista)
# else:
#     print(lista.append(lista))
    
    
    # for p in peso:
    #     print(p, peso)
    # if peso:
    #     pesos =
    #     print(pesos)
    # if peso > 0:
    #     i = peso
    #     pesos += i
    # pesos = list(i)
    # print(peso)
    # lista = [i]
    # maior += peso
    # menor += peso
    # pass
    # print(menor)
    # print(i)
# else:
    # print('\n',pesos)
    # print(f'Maior peso: {max(maior)}\n'
    #       f'Menor peso: {min(menor)}')
